version: '2'
services:
    coursework-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/coursework/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=coursework
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
